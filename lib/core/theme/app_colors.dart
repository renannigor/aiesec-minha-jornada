import 'package:flutter/material.dart';

class AppColors {
  // Cor Principal da Marca
  static const Color primaryBlue = Color(0xFF037EF3);

  // Cores de Texto
  static const Color textPrimary = Color(0xFF1F2937); // Escuro para títulos
  static const Color textSecondary = Color(0xFF6B7280); // Cinza para descrições

  // Cores de Fundo e Elementos Estruturais
  static const Color backgroundWhite = Color(0xFFFFFFFF);
  static const Color backgroundLight = Color(
    0xFFF9FAFB,
  ); // Fundos de botões/cards desativados

  // Cores de Bordas
  static const Color borderDefault = Color(0xFFE5E7EB);
  static const Color borderFocus = primaryBlue;
}
